#include<iostream.>
#include<string.h>
#include<conio.h>
int main()
{
	char str[10], copy str[10]; 
	int x;
	cout<<"program to cheack if astring entered by user is palindromeor not";
	cin>>str;
	strrev
	strcpy(copy,str);
	strrev(str);
	x=(str cmp copy,str);
	if(x==0);
{
	cout<<"the entered string is palindrome";
}
else
cout<<"the entered string is not palinder";
getch();
return 0 ;
}

